import 'package:flutter/material.dart';
class ResetCodePage extends StatelessWidget {
  const ResetCodePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text ("Reset Code Page")),
    );
  }
}